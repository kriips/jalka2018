// @flow

import * as React from "react";

export default class UserPredictionRow extends React.Component<{}> {
  render() {
    console.log("rowprop", this.props);
    // const list = this.props.matches.edges.map(edge => {
    //   <UserPredictionRow match={edge.node} />
    // });
    //
    return 1;
  }
}
